# CC Father 首次启动说明

## 当前技术栈

CC Father 当前以 cc-switch 为主项目，使用 Tauri 2、Rust、React、TypeScript、Vite 和 pnpm。cc-boot 位于 `integrations/cc-boot`，作为后续整合子包。

## 本机已完成的环境安装

| 工具 | 版本或组件 | 用途 |
| --- | --- | --- |
| Node.js | v24.15.0 | 前端构建与工具脚本 |
| pnpm | v10.12.3 | 工作区依赖管理 |
| Rust | 1.95.0（MSVC） | Tauri/Rust 后端编译 |
| Rust 组件 | rustfmt、clippy | 格式化和静态检查 |
| Visual Studio 2022 Build Tools | C++ Build Tools 工作负载 | Windows MSVC 链接器与原生依赖编译 |

项目的 Rust 版本由 [rust-toolchain.toml](rust-toolchain.toml) 固定为 `1.95`，不要随意改用其他版本。

## 已执行验证

```powershell
pnpm install
pnpm typecheck
cargo check --manifest-path src-tauri\Cargo.toml
pnpm build
```

- `pnpm install` 已完成，根项目与 `integrations/cc-boot` workspace 依赖已安装。
- `pnpm typecheck` 已通过。
- `cargo check` 已通过，Tauri/Rust 后端可在当前 Windows MSVC 环境编译。
- `pnpm build` 已完成前端 Vite 生产构建和 Rust release 编译，生成了 `src-tauri/target/release/cc-switch.exe`。
- Windows MSI 打包阶段未完成：WiX 的 `light.exe` 执行失败。该问题不影响已生成的 release 可执行文件，但在解决前不能交付 MSI 安装包。
- 当前系统中已有一个 `cc-switch` 桌面进程在运行。它来自已安装目录；项目构建产物与已安装版本互不覆盖。

## 日常启动

在项目根目录执行：

```powershell
pnpm dev
```

该命令会先启动 Vite 开发服务器（默认 `http://localhost:3000`），随后启动 Tauri 桌面窗口。关闭终端或按 `Ctrl+C` 会结束开发服务和应用。

仅调试前端页面时：

```powershell
pnpm dev:renderer
```

## 构建发布包

```powershell
pnpm build
```

Windows 安装包与可执行产物会生成在 `src-tauri/target/release/bundle/`。该目录已被 `.gitignore` 忽略。

当前已确认生成的可执行文件位于：

```text
src-tauri/target/release/cc-switch.exe
```

如果已安装的 CC Switch 正在运行，直接启动这个 release 文件会被单实例机制接管，不会产生第二个进程。需要验证本次构建窗口时，先正常退出已安装版本，再执行上述 release 文件或运行 `pnpm dev`。

MSI 安装包依赖 WiX 的 `candle.exe` 和 `light.exe`。本机首次自动下载 WiX 后，`light.exe` 返回失败；重新构建前应先检查该可执行文件是否能在当前 Windows 环境中正常运行，并保留完整构建输出以定位原因。

## 常见问题

### 找不到 rustc、cargo 或 rustup

关闭并重新打开 PowerShell，使 Rustup 写入的环境变量生效。若仍未生效，当前会话可以临时执行：

```powershell
$env:Path = "$env:USERPROFILE\.cargo\bin;$env:Path"
```

### 找不到 pnpm

重新打开终端后执行：

```powershell
pnpm --version
```

若命令不存在，可重新安装项目指定版本：

```powershell
npm install --global pnpm@10.12.3
```

### Rust 链接阶段提示缺少 link.exe 或 MSVC

安装 Visual Studio 2022 Build Tools，并包含“使用 C++ 的桌面开发”或 `Microsoft.VisualStudio.Workload.VCTools` 工作负载；安装后重新打开 PowerShell。

### 首次构建时间很长

这是正常现象。Tauri 会首次下载并编译 WebView、SQLite、加密、更新器等 Rust crates。后续构建会使用 `src-tauri/target/` 缓存，速度会显著提升。

### 浏览器数据或 bundle 体积警告

当前构建会提示 `caniuse-lite` 数据较旧和 JavaScript chunk 大于 500 kB；这两项是上游项目警告，不影响当前类型检查、Rust 编译和启动。后续可单独安排依赖更新与代码分包优化。
