# 产物构建

本项目使用 Tauri 2 打包。请在目标操作系统上构建对应的原生安装包：Windows、macOS 和 Linux 的桌面运行时与打包工具不能可靠地由另一种操作系统交叉构建。

## 通用准备

- Node.js 20 或更高版本
- Corepack 与 pnpm 10.12.3
- Rust 1.85 或更高版本
- 在项目根目录执行命令

首次构建先安装依赖：

```powershell
corepack enable
pnpm install --frozen-lockfile
```

打包会自动执行 `pnpm run build:renderer`，生成前端资源后再构建 Rust 应用。

## Windows

在 Windows 上执行：

```powershell
pnpm tauri build --bundles msi --config src-tauri/tauri.unsigned.conf.json
```

或双击运行项目根目录的 `build-pack.cmd`。

该命令生成可安装的未签名 MSI，并关闭自动更新签名产物，适合本地测试和内部使用。若要生成可用于自动更新的正式发布包，请配置 `TAURI_SIGNING_PRIVATE_KEY` 后使用：

```powershell
pnpm tauri build --bundles msi
```

产物位于：

```text
src-tauri\target\release\bundle\msi\cc-launch_3.20.0_x64_en-US.msi
```

文件名中的版本和架构会随构建环境变化。MSI 为每用户安装包，双击后在安装向导的目录页面选择安装位置；默认位置在 `%LOCALAPPDATA%\Programs\cc-launch`。

构建 Windows ARM64 安装包：

```powershell
rustup target add aarch64-pc-windows-msvc
pnpm tauri build --target aarch64-pc-windows-msvc --bundles msi
```

ARM64 产物位于：

```text
src-tauri\target\aarch64-pc-windows-msvc\release\bundle\msi\
```

## macOS

在 macOS 上执行：

```bash
pnpm tauri build --target universal-apple-darwin
```

Apple Silicon 与 Intel 的通用产物位于：

```text
src-tauri/target/universal-apple-darwin/release/bundle/
```

分发给普通用户建议使用 `.dmg`。双击 DMG 后将 `cc-launch.app` 拖到 Applications 文件夹完成安装。macOS 的标准 DMG 安装模型不提供 Windows MSI 那样的安装目录选择；若用户需要放在其他位置，可将应用拖到任意有写权限的应用目录。

正式公开分发前需要配置 Apple Developer ID 证书并完成公证，否则 Gatekeeper 会提示来源未验证。仓库的 GitHub Release 工作流会在已配置 Apple 签名和公证密钥时生成经过签名与公证的 DMG。

## Linux

在 Debian/Ubuntu 上先安装系统依赖：

```bash
sudo apt-get update
sudo apt-get install -y build-essential pkg-config libssl-dev libgtk-3-dev librsvg2-dev libayatana-appindicator3-dev libwebkit2gtk-4.1-dev libsoup-3.0-dev rpm patchelf
```

如果发行版没有 `libwebkit2gtk-4.1-dev` 或 `libsoup-3.0-dev`，请使用该发行版提供的兼容版本。

构建 AppImage、Deb 和 RPM：

```bash
pnpm tauri build --bundles appimage,deb,rpm
```

产物位于：

```text
src-tauri/target/release/bundle/appimage/
src-tauri/target/release/bundle/deb/
src-tauri/target/release/bundle/rpm/
```

使用方式：

- `.AppImage`：赋予执行权限后双击运行；文件放置在哪个目录，就相当于选择了使用目录。
- `.deb`：双击由软件中心安装，或执行 `sudo apt install ./<文件名>.deb`；系统软件包固定安装到系统目录，不能在安装器中选择任意目录。
- `.rpm`：双击由发行版软件中心安装，或执行 `sudo dnf install ./<文件名>.rpm`；同样固定安装到系统目录。

## 自动构建所有平台

仓库的 `.github/workflows/release.yml` 已配置 Windows x64、Windows ARM64、Linux x64、Linux ARM64 和 macOS 构建。推送形如 `v3.20.0` 的标签会触发对应 runner，生成可发布的三端产物。

```bash
git tag v3.20.0
git push origin v3.20.0
```

此操作会创建远程发布，只有确认版本号和发布权限后再执行。
