# 05 Cursor 能力扩展

## 目标

按风险逐项支持历史消息、图片、reasoning、MCP 和工具生命周期；未支持项继续前置拒绝。

## 前置

- 基础流、取消和恢复均通过。
- 每项能力有脱敏 fixture 和内部契约。

## 实施

1. 先实现历史 hydrate 与大小限制。
2. 再实现图片/blob schema 校验。
3. 再实现 reasoning/effort 映射。
4. 最后实现 MCP/工具执行及副作用确认。

## 退出门

- 每项能力都有保留、拒绝、错误和取消测试。
- 不支持的字段仍返回机器可读 `NOT_IMPLEMENTED`。
