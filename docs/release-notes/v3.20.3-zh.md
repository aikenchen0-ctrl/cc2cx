# cc2cx v3.20.3

本版本改善旧 CC Switch 配置同步体验。

- 打开 Agent 安装页时自动扫描旧 CC Switch 数据目录、备份目录、下载、桌面和文档目录。
- 只展示包含 `CC Switch SQLite 导出` 标记的 SQL 文件。
- 自动选中最近修改的有效导出文件。
- 保留手动选择其他 SQL 文件的按钮。
- 导入前继续使用临时数据库校验和自动备份，旧数据默认不删除。
