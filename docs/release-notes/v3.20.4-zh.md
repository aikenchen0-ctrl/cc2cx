# cc2cx v3.20.4

本版本修复 Windows 下 WorkBuddy 签名误判，并优化旧 CC Switch 卸载入口。

- WorkBuddy 强制调用系统 Windows PowerShell 5.1 校验 Authenticode，避免 `Microsoft.PowerShell.Security` 模块加载失败。
- Windows 优先启动检测到的旧 CC Switch 卸载程序或卸载快捷方式。
- 没有可靠卸载入口时，才打开系统应用卸载页面作为兜底。
- 旧 CC Switch 配置和备份默认保留，不会被自动删除。
