# CC Father 全量合并项目规划

## 定位

CC Father 以根目录的 `cc-switch`（Tauri + React + Rust）为源头项目，不再采用 Electron，也不迁移到 Vue/Vuetify。`integrations/cc-boot` 是 pnpm workspace 子包，负责向主项目提供安装引导、初始化、诊断和更新能力。

一比一的验收口径是：功能、数据兼容、成功/失败/取消/重试行为和关键交互等价；内部实现可以把 cc-boot 的 TypeScript 逻辑通过 Rust command/service 或受控子进程接入 cc-switch，但不能用静态占位或静默降级代替。

## 仓库结构

```text
cc-father/
├─ src/                 # cc-switch React 前端，主项目
├─ src-tauri/           # cc-switch Rust 后端，主业务边界
├─ integrations/cc-boot # cc-boot 源码与测试，workspace 子包
├─ tests/               # 主项目测试
└─ docs/                # 原 cc-switch 文档
```

## cc-boot 接入范围

- 工具检测、安装和初始化：Claude Code、Codex、Gemini CLI、OpenCode、OpenClaw、CCR。
- `init`、`setup`、工具别名、`--all`/`--tools`、静默参数、Provider、API Key、URL、模型和语言参数。
- Provider 预设（国内/全球）、MCP 预设与初始化。
- `doctor`、`update`、`update --check`、`handoff`、`handoff --install`。
- 中英文文案和命令行兼容入口。

接入方式：主项目新增 Bootstrap command/service；UI 使用现有 cc-switch React 组件体系；安装和更新任务以事件流返回进度、日志、错误码、取消和重试状态。cc-boot 不单独启动第二个桌面应用。

## cc-switch 保留并作为主项目的能力

Provider 管理与切换、通用配置、双向 live 配置同步、原子写入和备份、MCP、Prompts、Skills、本地代理、协议转换、路由、健康检查、熔断、故障转移、用量与费用、请求日志、会话与工作区、Deep Link、云/WebDAV 同步、托盘、主题、开机启动、自动更新、多语言和 Windows/macOS/Linux 发布能力全部保留。

## 实施阶段

### P0：基线与契约（2 周）

冻结两个子项目 commit、版本和配置样例；建立工具/Provider/MCP/Prompt/Skill/代理/会话兼容矩阵；为 CLI 和 UI 建立行为、错误、数据快照。

### P1：cc-boot 作为 workspace 子包接入（3-4 周）

抽取 cc-boot 可复用的工具 registry、Provider presets、MCP presets、installer、doctor、update、handoff；定义 Rust command 与 TypeScript 子包之间的输入输出契约。

### P2：安装引导 UI（2-3 周）

在 cc-switch 现有导航和组件体系中加入安装向导、工具选择、Provider 配置、MCP 选择、权限确认、进度日志、取消/重试和结果校验。

### P3：数据迁移与交接（2 周）

兼容 cc-boot 配置、cc-switch SQLite、settings、backups、skills；实现只读扫描、迁移预览、自动备份、失败恢复和 handoff 幂等导入。

### P4：全量回归与发布（3-5 周）

运行类型检查、Rust/前端单测、安装器集成、跨平台权限、更新回滚、Deep Link、代理和真实配置文件回归测试；完成三平台打包冒烟。

## 时间估算

- 只接入安装、Provider 初始化、doctor、update：单人约 6-10 周。
- 包含全部 cc-boot 能力并完成数据迁移：单人约 3-4 个月。
- 做到完整稳定、跨平台发布和充分回归：单人约 4-6 个月，2-3 人约 2-3 个月。

## 关键风险

1. cc-switch 的 Rust/Tauri 与 cc-boot 的 Node CLI 进程边界需要明确，不能直接共享 UI 状态。
2. 工具配置格式和路径会随 CLI 版本变化，必须版本化适配器和 schema。
3. API Key 只能进入系统密钥环，不能通过 Renderer、普通日志或明文导出传播。
4. 安装、代理、同步和更新涉及网络/权限/回滚，所有失败必须显式反馈。

## 当前下一步

先完成 P0，不立即大规模改 UI：冻结版本，整理命令与服务契约，确认 `integrations/cc-boot` 的构建方式，再逐步接入主项目 Rust command 和 React 页面。
